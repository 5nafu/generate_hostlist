sudo: false
language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "pypy"
install:
  - pip install pybuilder coveralls codecov
  - pyb -v install_dependencies

script:
  - pyb -v -X verify analyze
after_success:
  - coveralls --verbose
  - codecov

